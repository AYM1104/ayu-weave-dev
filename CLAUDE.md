# CLAUDE.md (Repository Root)

## 目的

このファイルは、Claude Code / Codex がこのリポジトリで一貫した判断を行うための全体運用ルールです。
root では cross-cutting な方針のみを扱い、実装詳細は各ディレクトリ配下の `CLAUDE.md` に委譲します。

このリポジトリは、ウェディングアルバム作成プロダクト「Weave Album」のモノレポです。
Claude は「仕様・ADR・既存構成に整合する変更」を優先し、未承認の方針を勝手に確定しません。

## 正本の優先順位（上ほど強い）

1. `docs/adr/*.md`
2. `docs/specs/**/*.md`
3. `README.md`
4. 各ディレクトリ配下の `CLAUDE.md`
5. 実装コード内コメント / 一時メモ

矛盾がある場合は、上位ドキュメントを優先し、差分と影響範囲を明示して提案します。
暗黙に丸めず、必要なら ADR / specs 更新を提案します。

## プロダクト境界

### MVPとして扱うもの
- 写真アップロード
- アルバム仕様選択
- テンプレート選択
- テンプレートまたは自動整列によるレイアウト案作成
- 枠内での簡易写真調整
- プレビュー確認
- 注文内容確認・確定
- Stripe決済
- 印刷会社APIへの連携

### MVP外として扱うもの
- BtoB管理機能
- 複数人共同編集
- 高度な自由編集
- テンプレート外の自由レイアウト
- ネイティブアプリ前提の設計
- 他商材展開
- SaaS化や海外展開

MVP外の提案が必要な場合は、実装に混ぜず「別案」として分離して記述します。

## 必須ルール

- 新規提案・設計・実装は AWS 前提で扱う。
- ただし AWS 個別サービスが未決定の領域は、未決定のまま扱う。
- Firebase / GCP / Cloud Run / GCS 固有の新規前提を追加しない。
- 認証方式、実行基盤、DB詳細、IaC、監視方式など、ADR未確定の項目を勝手に確定しない。
- 仕様変更を伴う実装を行う場合、必要に応じて `docs/specs` または `docs/adr` の更新提案をセットで行う。
- モノレポ横断変更（frontend / backend / docs / infra）では、影響範囲を先に列挙してから変更する。
- 1つの変更で複数の意思決定を同時に確定しない。論点は分離する。

## 変更時の共通手順

1. 変更対象のコードだけでなく、関連する `docs/adr` / `docs/specs` / 下位 `CLAUDE.md` を確認する。
2. その変更が MVP 内か、MVP 外かを確認する。
3. 未決定事項を含むなら、実装確定ではなく比較案・保留論点として扱う。
4. frontend/backend をまたぐ場合は、API契約・型・エラーハンドリング・docs差分を確認する。
5. 実装後は、変更理由・影響範囲・未解決論点・必要テストを短くまとめる。

## Claude の出力ルール

変更提案・実装・レビューでは、常に次を明記します。

- 何を変えるか
- なぜ変えるか
- 影響範囲
- 未決定事項
- 追加で必要な docs / ADR / test
- 今回あえてやらないこと

破壊的変更や方針変更がある場合は、必ず「即時対応」と「後続対応」を分けて書きます。

## 禁止事項

- 未承認ADRを既成事実としてコードや文書に埋め込むこと
- specs と矛盾したまま実装だけ進めること
- MVP外機能を紛れ込ませること
- frontend / backend / docs の整合が必要な変更を片側だけで閉じること
- 暗黙の命名変更や責務変更を行うこと
- 大きなリファクタを無関係な変更に混ぜること

## スコープ別補助ルール

以下が存在する場合、この root ルールに追加して適用します。

- `docs/CLAUDE.md`
- `frontend/CLAUDE.md`
- `backend/CLAUDE.md`
- `infra/CLAUDE.md`

root は全体統治、下位ファイルは局所実装ルールを担当します。
局所ルールが root と矛盾する場合は、root ではなく上位正本（ADR / specs / README）を確認して差分を解消します。

## 推奨コマンド

- `/aws-adr-check`
- `/sync-specs`
- `/review-staged`

## 人間の最終判断が必要なもの

- AWS個別サービスの採用決定
- 認証方式の確定
- 実行基盤・IaC・監視方式の確定
- MVP範囲変更
- リリース判断
- セキュリティ / コスト / 速度の高コストなトレードオフ判断
