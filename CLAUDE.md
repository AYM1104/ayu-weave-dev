# CLAUDE.md

## 目的

このファイルは、このリポジトリで Claude Code が一貫した判断を行うための全体ルールを定義する。

このリポジトリは、ウェディングアルバム作成プロダクトのモノレポである。
Claude は「仕様・ADR・既存構成に整合する変更」を優先し、未承認の方針を勝手に確定しない。

root の `CLAUDE.md` は repo 全体に効く共通ルールのみを扱う。
実装詳細・局所ルールは各ディレクトリ配下の `CLAUDE.md` に委譲する。

---

## このリポジトリでの Claude の役割

Claude は以下を支援する。

- 仕様に沿った実装
- 仕様・コード・ドキュメントの整合維持
- 差分レビュー
- 影響範囲の整理
- docs / specs / ADR 更新提案
- 安全なリファクタと小さな改善

Claude は以下を勝手に確定しない。

- 未承認のアーキテクチャ判断
- 未決定の AWS サービス選定
- 認証方式の最終決定
- 破壊的な API / DB 変更
- MVP 範囲の拡張
- 大規模な構成変更

---

## 正本の優先順位

矛盾がある場合は、以下を上から優先する。

1. `docs/adr/*.md`
2. `docs/specs/**/*.md`
3. `README.md`
4. 各ディレクトリ配下の `CLAUDE.md`
5. 実装コード内コメント
6. 一時メモ・会話上の補足

運用ルール:

- 上位ドキュメントと矛盾する実装は行わない
- 矛盾を見つけた場合は、勝手に丸めず差分を明示する
- 実装で仕様変更が必要なら、コード変更だけで閉じず docs 更新も提案する
- 不明点がある場合は、既存仕様に最も保守的な解釈を採る

---

## プロダクト境界

### MVP として扱うもの

- 写真アップロード
- アルバム仕様選択
- テンプレート選択
- テンプレートまたは自動整列によるレイアウト案作成
- 枠内での簡易写真調整
- プレビュー確認
- 注文内容確認・確定
- Stripe 決済
- 印刷会社 API への連携

### MVP 外として扱うもの

- BtoB 管理機能
- 複数人共同編集
- 高度な自由編集
- テンプレート外の自由レイアウト
- ネイティブアプリ前提の設計
- 他商材展開
- SaaS 化や海外展開

ルール:

- MVP 外の機能を実装に混ぜない
- MVP 外の改善案を出す場合は、「別案」として分離して記述する
- MVP と非 MVP の境界が曖昧な場合は、拡張ではなく縮小側に倒して判断する

---

## 技術方針の大原則

- クラウド前提は AWS に統一する
- ただし、AWS の個別サービス未決定領域は未決定のまま扱う
- Firebase / GCP / Cloud Run / GCS などを新規前提として持ち込まない
- 実装前の仮決めを既成事実化しない
- 3人チーム運用に対して過剰な構成を持ち込まない
- まずは MVP を前に進めるための最小構成を優先する
- 将来拡張のためだけの抽象化を先に入れすぎない
- 実装より先に過剰な仕組み化を行わない

---

## 未決定事項の扱い

以下は、ADR や明示仕様がない限り Claude が勝手に確定してはならない。

- AWS 個別サービスの採用
- 認証方式
- バックエンド実行基盤
- IaC の採用詳細
- 監視・ログ・通知基盤
- 非機能要件に強く影響する構成
- データ保存戦略の重要決定
- セキュリティ方針の最終形
- 外部 API 連携方式の重要変更

不明なときのルール:

- 断定せず「候補」「比較案」「保留論点」として扱う
- 実装を進める場合は、未決定事項に依存しない範囲に留める
- 方針変更が必要なら ADR 追加または更新提案を優先する

---

## 人間の承認なしに確定しないもの

以下は人間の承認が必要であり、Claude が単独で確定しない。

- 新規外部依存の追加
- 主要依存の置き換え
- API / DB スキーマの破壊的変更
- ディレクトリ構成の大幅変更
- 認証・決済・インフラ境界に関わる方針変更
- セキュリティ・権限・個人情報の扱い変更
- MVP 範囲変更
- リリース判断

Claude は必要なら変更案を出してよいが、承認待ちの論点であることを明示する。

---

## モノレポ運用の基本原則

この repo はモノレポとして扱う。
各領域の責務を壊さないこと。

想定責務:

- `frontend/`: UI、画面、フロントエンド状態、クライアント側の体験
- `backend/`: API、業務ロジック、データ処理、外部連携
- `infra/`: インフラ定義、環境構成、運用基盤
- `docs/`: specs、ADR、設計文書、運用文書

ルール:

- 局所修正に見えても、横断影響があるなら先に列挙する
- frontend / backend をまたぐ変更では、API 契約・型・エラーハンドリング・docs 差分を確認する
- infra 前提の変更は、コードだけでなく docs / ADR との整合を確認する
- docs は実装の後追いメモではなく、仕様の正本として扱う

---

## 変更時の共通手順

Claude は変更前・変更中・変更後に最低限以下を守る。

### 1. 変更前

- 変更対象コードだけでなく、関連する `docs/adr`、`docs/specs`、`README.md`、配下 `CLAUDE.md` を確認する
- その変更が MVP 内か MVP 外かを判定する
- 未決定事項に触れるかどうかを確認する
- 影響範囲が frontend / backend / infra / docs のどこに及ぶかを整理する

### 2. 変更中

- 変更は原則として 1 つの論点に閉じる
- 機能追加、仕様変更、構成変更、リファクタを無関係に混在させない
- 必要最小限の差分で目的を達成する
- 暗黙の命名変更や責務変更を行わない
- 無関係な整形や広範囲な書き換えを混ぜない

### 3. 変更後

- 何を変えたか
- なぜ変えたか
- 影響範囲
- 未決定事項
- 必要な docs / ADR / test の追従
- 今回あえてやらないこと

を短く整理して示す。

---

## docs / specs / ADR 更新ルール

### specs を更新または更新提案すべきケース

- API 契約に変更がある
- データモデルの意味が変わる
- 画面仕様やユーザー体験に変更がある
- 業務フローや注文フローの意味が変わる
- エラーケースや制約条件が変わる

### ADR を更新または追加提案すべきケース

- アーキテクチャ判断が変わる
- クラウド / 実行基盤 / 認証 / 監視 / 永続化などの重要方針が変わる
- 長期的な技術選定の理由を記録すべき変更がある
- 今後の実装方針に継続的影響を与える決定がある

ルール:

- API 契約またはデータモデル変更がある場合、実装だけで閉じない
- 関連する `docs/specs` の更新または更新提案を必須とする
- ADR 級の変更を、コードコメントだけで済ませない

---

## レビュー時の観点

Claude がレビューや差分確認を行うときは、少なくとも以下を見る。

- 仕様整合性
- MVP 逸脱の有無
- AWS 方針との整合性
- 破壊的変更の有無
- API / 型 / エラーハンドリング整合
- docs 更新漏れ
- テスト不足
- 無関係な差分混入
- 過剰設計
- 将来の保守負債

レビューでは、単に「よさそう」と言わず以下を分けて示す。

- 問題なし
- 修正推奨
- 要承認
- 今回見送るべき提案

---

## 実装方針

- 小さく変更する
- 既存構成に沿う
- 既存命名に合わせる
- 先回り抽象化を避ける
- 複雑な仕組みより単純な仕組みを優先する
- テスト可能性を落とさない
- エラー時の振る舞いを曖昧にしない
- 将来のためだけの汎化を先に作りすぎない

禁止ではないが慎重に扱うもの:

- 共通化のための共通化
- 早すぎるマイクロサービス的分割
- 使う予定のない拡張ポイント
- まだ未確定な infra 前提に依存した実装
- 便利そうという理由だけの依存追加

---

## 依存追加ルール

新しいライブラリ、SDK、外部サービスを導入する場合は、以下を確認する。

- 既存依存で代替できないか
- MVP に本当に必要か
- 保守コストに見合うか
- セキュリティ / ライセンス / ロックインの懸念はないか
- チームの理解コストが過大でないか

ルール:

- 依存追加は自動で既成事実化しない
- 追加する場合は理由、代替案、影響範囲を明示する
- 大きい依存ほど人間承認前提で扱う

---

## テストと検証の原則

Claude は変更に応じて、必要な確認を考慮する。

最低限の観点:

- 壊れていないか
- 期待する入出力になっているか
- 主要フローを壊していないか
- エラー時の扱いが妥当か
- docs / specs と矛盾していないか

ルール:

- 変更内容に見合うテスト観点を示す
- テスト未実施なら未実施と明記する
- 重要な回帰リスクがある場合、確認方法を提案する
- 実装だけして検証観点を出さないことを避ける

---

## セキュリティと安全性

- secrets をコードや docs に埋め込まない
- 本番値・鍵・トークン・個人情報を仮値としても書かない
- セキュリティに関わる変更は軽く扱わない
- 権限境界を曖昧にしない
- 決済、認証、個人情報、ファイルアップロードは特に慎重に扱う
- 安全性に疑義がある場合は、実装断定より保留・確認を優先する

---

## 出力ルール

Claude は提案・実装・レビュー時に、必要に応じて以下を明示する。

- 何を変えるか
- なぜ変えるか
- 影響範囲
- 未決定事項
- 必要な docs / ADR / test
- 今回あえてやらないこと

破壊的変更、構成変更、方針変更がある場合は以下を分ける。

- 今すぐやること
- 承認後にやること
- 今回やらないこと

不確実な内容は断定口調で既成事実化しない。

---

## 禁止事項

- 未承認 ADR を既成事実としてコードや文書に埋め込むこと
- specs と矛盾したまま実装だけ進めること
- MVP 外機能を紛れ込ませること
- frontend / backend / docs の整合が必要な変更を片側だけで閉じること
- 無関係な大規模リファクタを同じ変更に混ぜること
- 未決定の infra 方針を仮決めのまま固定化すること
- 依存追加を理由説明なしに行うこと
- 不要な抽象化や過剰設計を持ち込むこと
- 実装根拠を示さずに大きな方針変更を行うこと

---

## ディレクトリ配下の CLAUDE.md との関係

以下が存在する場合、この root ルールに追加して適用する。

- `docs/CLAUDE.md`
- `frontend/CLAUDE.md`
- `backend/CLAUDE.md`
- `infra/CLAUDE.md`

役割分担:

- root `CLAUDE.md`: 全体統治、優先順位、変更原則、承認境界
- 配下 `CLAUDE.md`: 実装詳細、局所ルール、レイヤ固有の注意点

局所ルールが root や上位正本と矛盾する場合は、勝手に局所ルールを優先しない。
`docs/adr`、`docs/specs`、`README.md` を確認し、差分を明示する。

---

## 推奨コマンド

利用可能であれば、以下を優先して使う。

- `/aws-adr-check`
- `/sync-specs`
- `/review-staged`

これらが未整備または不足している場合でも、同等の観点で確認する。

---

## Claude の基本姿勢

- まず repo の正本を読む
- 仕様に従う
- 小さく変える
- 過剰設計を避ける
- 未決定事項を勝手に埋めない
- 必要な docs 更新を見逃さない
- 人間が握るべき判断を侵食しない
- 3人チームで継続運用できる現実解を優先する

この repo では、賢そうな複雑化より、整合性の高い小さな前進を優先する。
