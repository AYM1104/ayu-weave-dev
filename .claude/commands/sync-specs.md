# /sync-specs

目的: docs/specs と ADR のドリフトを、未決定事項を確定せずに最小修正で同期する。

手順:
1. 変更対象 specs を列挙する。
2. `docs/adr/0001-cloud-platform-aws.md` と衝突する記述を抽出する。
3. 衝突を 重大 / 中 / 軽微 で分類する。
4. 「今すぐ直す」「後続ADR待ち」に分けて修正案を作る。
5. 未決定事項（認証/実行基盤/DB/IaC）は未決定のまま明記する。

出力テンプレート:
- 変更ファイル
- 重大/中/軽微の不整合
- 今すぐ直す項目
- 後続ADR待ち項目
